'use client'

import { useEffect, useMemo, useState } from 'react'
import { Activity, AlertCircle, Bell, ClipboardList, FileBarChart, FolderKanban, Gauge, Globe2, Handshake, LayoutDashboard, Menu, Network, Plus, Search, Settings, ShieldCheck, Users, X, CheckCircle2, Clock3, MapPin, SlidersHorizontal, Trash2 } from 'lucide-react'
import { RecordForm, type Field } from './record-form'

type RecordItem = { id: number; status?: string; [key: string]: string | number | undefined }
type ModuleConfig = { label: string; icon: typeof LayoutDashboard; singular: string; fields: Field[]; columns: string[]; seed?: RecordItem[] }

const configs: ModuleConfig[] = [
  { label: 'Projects', icon: FolderKanban, singular: 'project', fields: [{ key: 'name', label: 'Project name', placeholder: 'Bluewater Solar Corridor', required: true }, { key: 'area', label: 'Geographic area', placeholder: 'Mahlangu District', required: true }, { key: 'stage', label: 'Stage', placeholder: 'Planning', required: true }, { key: 'status', label: 'Status', placeholder: 'On track', required: true }, { key: 'description', label: 'Description', placeholder: 'Short project description' }], columns: ['name', 'area', 'stage', 'status'], seed: [{ id: 1, name: 'North Ridge Wind Farm', area: 'Mahlangu District', stage: 'Construction', status: 'On track', description: 'Renewable energy infrastructure programme' }] },
  { label: 'Geographic Areas', icon: Globe2, singular: 'area', fields: [{ key: 'name', label: 'Area name', placeholder: 'Kopano Municipality', required: true }, { key: 'region', label: 'Region', placeholder: 'Northern Corridor', required: true }, { key: 'areaType', label: 'Area type', placeholder: 'Municipality' }, { key: 'notes', label: 'Notes', placeholder: 'Primary programme area' }], columns: ['name', 'region', 'areaType', 'notes'], seed: [{ id: 1, name: 'Mahlangu District', region: 'Northern Corridor', areaType: 'District', notes: 'Primary project area' }] },
  { label: 'Stakeholders', icon: Users, singular: 'stakeholder', fields: [{ key: 'name', label: 'Organisation name', placeholder: 'Riverbend Forum', required: true }, { key: 'type', label: 'Stakeholder type', placeholder: 'Community forum', required: true }, { key: 'area', label: 'Area', placeholder: 'Kopano Municipality', required: true }, { key: 'contact', label: 'Contact role', placeholder: 'Chairperson' }, { key: 'status', label: 'Relationship status', placeholder: 'Active', required: true }], columns: ['name', 'type', 'area', 'contact', 'status'], seed: [{ id: 1, name: 'Riverbend Forum', type: 'Community forum', area: 'Mahlangu District', contact: 'Chairperson', status: 'Active' }] },
  { label: 'Governance Institutions', icon: Network, singular: 'institution', fields: [{ key: 'name', label: 'Institution name', placeholder: 'Kopano Municipal Council', required: true }, { key: 'level', label: 'Government level', placeholder: 'Municipal', required: true }, { key: 'area', label: 'Area', placeholder: 'Kopano Municipality' }, { key: 'contact', label: 'Primary contact role', placeholder: 'Planning office' }, { key: 'status', label: 'Status', placeholder: 'Active' }], columns: ['name', 'level', 'area', 'contact', 'status'], seed: [{ id: 1, name: 'Mahlangu District Council', level: 'District', area: 'Mahlangu District', contact: 'Infrastructure directorate', status: 'Active' }] },
  { label: 'Engagements', icon: Handshake, singular: 'engagement', fields: [{ key: 'title', label: 'Engagement title', placeholder: 'Ward 4 construction briefing', required: true }, { key: 'project', label: 'Project', placeholder: 'North Ridge Wind Farm', required: true }, { key: 'area', label: 'Area', placeholder: 'Mahlangu District' }, { key: 'date', label: 'Date', placeholder: '2026-09-24', type: 'date', required: true }, { key: 'participants', label: 'Participants', placeholder: '25' }, { key: 'status', label: 'Status', placeholder: 'Scheduled', required: true }], columns: ['title', 'project', 'area', 'date', 'participants', 'status'], seed: [{ id: 1, title: 'Construction briefing', project: 'North Ridge Wind Farm', area: 'Mahlangu District', date: '2026-09-12', participants: '18', status: 'Completed' }] },
  { label: 'Grievances', icon: AlertCircle, singular: 'grievance', fields: [{ key: 'title', label: 'Issue summary', placeholder: 'Access road dust concerns', required: true }, { key: 'project', label: 'Project', placeholder: 'North Ridge Wind Farm', required: true }, { key: 'area', label: 'Area', placeholder: 'Mahlangu District' }, { key: 'priority', label: 'Priority', placeholder: 'Medium', required: true }, { key: 'status', label: 'Status', placeholder: 'Open', required: true }, { key: 'resolution', label: 'Resolution / next step', placeholder: 'Investigation scheduled' }], columns: ['title', 'project', 'area', 'priority', 'status', 'resolution'], seed: [{ id: 1, title: 'Access road dust concerns', project: 'North Ridge Wind Farm', area: 'Mahlangu District', priority: 'High', status: 'Open', resolution: 'Investigation scheduled' }] },
  { label: 'Impact Monitoring', icon: Activity, singular: 'indicator', fields: [{ key: 'name', label: 'Indicator name', placeholder: 'Local employment', required: true }, { key: 'project', label: 'Project', placeholder: 'North Ridge Wind Farm', required: true }, { key: 'area', label: 'Area', placeholder: 'Mahlangu District' }, { key: 'value', label: 'Current measurement', placeholder: '68%', required: true }, { key: 'unit', label: 'Unit', placeholder: '%' }, { key: 'status', label: 'Status', placeholder: 'On track', required: true }, { key: 'measuredOn', label: 'Measured on', placeholder: '2026-09-17', type: 'date' }], columns: ['name', 'project', 'value', 'unit', 'status', 'measuredOn'], seed: [{ id: 1, name: 'Local employment', project: 'North Ridge Wind Farm', area: 'Mahlangu District', value: '68', unit: '%', status: 'On track', measuredOn: '2026-09-17' }] },
]
const nav = [{ label: 'Dashboard', icon: LayoutDashboard }, ...configs.map(({ label, icon }) => ({ label, icon })), { label: 'Reports', icon: FileBarChart }, { label: 'Settings', icon: Settings }]

export function TrustLedgerShell() {
  const [active, setActive] = useState('Dashboard'); const [mobileOpen, setMobileOpen] = useState(false); const [query, setQuery] = useState(''); const [modal, setModal] = useState(false)
  const [records, setRecords] = useState<Record<string, RecordItem[]>>(() => Object.fromEntries(configs.map((config) => [config.label, config.seed ?? []])))
  const [hydrated, setHydrated] = useState(false); const [saveState, setSaveState] = useState<'idle' | 'saving' | 'saved' | 'error'>('idle')
  const [preferences, setPreferences] = useState({ compact: false, notifications: true, defaultReport: 'Project Summary' })
  const config = configs.find((item) => item.label === active)
  useEffect(() => { try { const stored = window.localStorage.getItem('trustledger.records.v1'); if (stored) setRecords(JSON.parse(stored)); const savedPrefs = window.localStorage.getItem('trustledger.preferences.v1'); if (savedPrefs) setPreferences(JSON.parse(savedPrefs)) } catch { setSaveState('error') } finally { setHydrated(true) } }, [])
  useEffect(() => { if (!hydrated) return; setSaveState('saving'); try { window.localStorage.setItem('trustledger.records.v1', JSON.stringify(records)); window.localStorage.setItem('trustledger.preferences.v1', JSON.stringify(preferences)); setSaveState('saved') } catch { setSaveState('error') } }, [records, preferences, hydrated])
  const addRecord = (record: Record<string, string>) => { if (!config) return; setRecords((current) => ({ ...current, [config.label]: [...(current[config.label] ?? []), { ...record, id: Date.now() }] })); setModal(false) }
  const title = active === 'Dashboard' ? 'Good morning, Alex' : active; const description = active === 'Dashboard' ? 'Here’s your stakeholder relations overview for today.' : active === 'Settings' ? 'Configure local workspace preferences and data controls.' : active === 'Reports' ? 'Generate evidence-based views from current workspace data.' : `Manage and monitor ${active.toLowerCase()} across your programmes.`
  return <div className={`min-h-screen bg-[#f7f9fb] text-slate-900 ${preferences.compact ? 'text-[13px]' : ''}`}>
    <aside className={`fixed inset-y-0 left-0 z-30 flex w-64 flex-col border-r border-slate-200 bg-[#102a43] text-slate-200 transition-transform lg:translate-x-0 ${mobileOpen ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex h-20 items-center justify-between border-b border-white/10 px-6"><div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-lg bg-teal-400 text-[#102a43]"><Gauge size={21} /></div><div><p className="text-[17px] font-semibold text-white">TrustLedger</p><p className="text-[10px] font-medium uppercase tracking-[0.18em] text-teal-300">SRM · V2 Experimental</p></div></div><button onClick={() => setMobileOpen(false)} className="lg:hidden" aria-label="Close navigation"><X size={20} /></button></div><nav className="flex-1 overflow-y-auto px-3 py-6" aria-label="Main navigation"><p className="px-3 pb-3 text-[10px] font-semibold uppercase tracking-[0.16em] text-slate-500">Workspace</p><div className="flex flex-col gap-1">{nav.map((item) => { const Icon = item.icon; return <button key={item.label} onClick={() => { setActive(item.label); setMobileOpen(false) }} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm ${active === item.label ? 'bg-teal-400 font-semibold text-[#102a43]' : 'font-medium text-slate-300 hover:bg-white/10 hover:text-white'}`}><Icon size={17} /><span>{item.label}</span>{item.label !== 'Dashboard' && records[item.label] && <span className="ml-auto text-[10px] opacity-60">{records[item.label].length}</span>}</button> })}</div></nav><div className="border-t border-white/10 p-4"><div className="rounded-lg bg-white/5 p-3"><div className="flex items-center gap-2 text-xs font-medium text-teal-300"><ShieldCheck size={14} /> Prototype environment</div><p className="mt-1.5 text-[11px] leading-4 text-slate-400">Local workspace data only. No live connections.</p></div></div></aside>
    <div className="lg:pl-64"><header className="sticky top-0 z-20 flex h-20 items-center justify-between border-b border-slate-200 bg-white/95 px-5 backdrop-blur sm:px-8"><div className="flex items-center gap-3"><button onClick={() => setMobileOpen(true)} className="text-slate-500 lg:hidden" aria-label="Open navigation"><Menu size={22} /></button><div><p className="text-xs font-medium text-slate-400">TrustLedger SRM <span className="mx-1">/</span> {active}</p><p className="mt-1 text-sm font-semibold text-slate-800">V2 Experimental Build</p></div></div><div className="flex items-center gap-4"><div className="relative hidden md:block"><Search className="absolute left-3 top-2.5 text-slate-400" size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search workspace" className="h-9 w-56 rounded-lg border border-slate-200 bg-slate-50 pl-9 pr-3 text-sm outline-none focus:border-teal-400" aria-label="Search workspace" /></div>{preferences.notifications && <Bell size={19} className="text-slate-500" />}<span className={`hidden text-[11px] font-medium sm:inline ${saveState === 'error' ? 'text-rose-600' : saveState === 'saving' ? 'text-amber-600' : 'text-slate-400'}`} role="status">{saveState === 'error' ? 'Save error' : saveState === 'saving' ? 'Saving…' : hydrated ? 'Saved locally' : 'Loading records…'}</span><div className="flex size-8 items-center justify-center rounded-full bg-[#d9f0ed] text-xs font-bold text-[#147d76]">AM</div></div></header><main className="mx-auto max-w-[1440px] px-5 py-8 sm:px-8"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-sm font-medium text-teal-700">Tuesday, 17 September 2026</p><h1 className="text-2xl font-semibold tracking-tight text-slate-950">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p></div>{config && <button onClick={() => setModal(true)} className="inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-[#147d76] px-4 text-sm font-semibold text-white shadow-sm hover:bg-[#106b65]"><Plus size={16} /> Add {config.singular}</button>}</div>{active === 'Dashboard' ? <Dashboard records={records} onNavigate={setActive} /> : active === 'Reports' ? <Reports records={records} defaultType={preferences.defaultReport} /> : active === 'Settings' ? <SettingsView preferences={preferences} setPreferences={setPreferences} records={records} setRecords={setRecords} /> : config ? <ModuleView config={config} records={records[active] ?? []} query={query} /> : null}</main></div>{modal && config && <RecordForm title={`Add ${config.singular}`} fields={config.fields} onCancel={() => setModal(false)} onSave={addRecord} />}</div>
}

function Dashboard({ records, onNavigate }: { records: Record<string, RecordItem[]>; onNavigate: (value: string) => void }) { const activeProjects = records.Projects.length; const stakeholders = records.Stakeholders.length; const grievances = records.Grievances.filter((item) => item.status?.toLowerCase() === 'open').length; const engagements = records.Engagements.length; const cards = [{ label: 'Active Projects', value: activeProjects, Icon: FolderKanban, target: 'Projects' }, { label: 'Registered Stakeholders', value: stakeholders, Icon: Users, target: 'Stakeholders' }, { label: 'Open Grievances', value: grievances, Icon: AlertCircle, target: 'Grievances' }, { label: 'Engagements Logged', value: engagements, Icon: Handshake, target: 'Engagements' }]; return <div className="flex flex-col gap-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({ label, value, Icon, target }) => <button key={label} onClick={() => onNavigate(target)} className="rounded-xl border border-slate-200 bg-white p-5 text-left shadow-sm hover:border-teal-300"><div className="flex items-start justify-between"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-3 text-3xl font-semibold tracking-tight text-slate-950">{value}</p></div><div className="rounded-lg bg-teal-50 p-2.5 text-teal-700"><Icon size={19} /></div></div><p className="mt-3 text-xs font-medium text-slate-500">Select to manage records</p></button>)}</div><div className="grid gap-6 xl:grid-cols-2"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="text-sm font-semibold">Workspace activity</h2><p className="mt-1 text-xs text-slate-500">Recent records across every module</p></div><ClipboardList size={18} className="text-teal-600" /></div><div className="mt-5 flex flex-col gap-3">{Object.entries(records).filter(([, items]) => items.length).map(([name, items]) => <div key={name} className="flex items-center justify-between rounded-lg bg-slate-50 px-3 py-3"><div><p className="text-sm font-medium">{name}</p><p className="mt-0.5 text-xs text-slate-500">{items.length} recorded {items.length === 1 ? 'item' : 'items'}</p></div><CheckCircle2 size={17} className="text-teal-600" /></div>)}</div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><MapPin size={18} className="text-teal-600" /><h2 className="text-sm font-semibold">Operating principles</h2></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{['Traceable records', 'Linked relationships', 'Evidence-led reporting'].map((item) => <div key={item} className="rounded-lg border border-slate-100 p-3"><p className="text-sm font-medium">{item}</p><p className="mt-1 text-xs leading-5 text-slate-500">Built into this experimental workspace.</p></div>)}</div></section></div></div> }

function ModuleView({ config, records, query }: { config: ModuleConfig; records: RecordItem[]; query: string }) { const [filter, setFilter] = useState('All'); const statuses = [...new Set(records.map((record) => String(record.status ?? '')).filter(Boolean))]; const visible = useMemo(() => records.filter((record) => Object.values(record).some((value) => String(value ?? '').toLowerCase().includes(query.toLowerCase()))).filter((record) => filter === 'All' || record.status === filter), [records, query, filter]); return <section className="rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-4 border-b border-slate-100 px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-sm font-semibold">{config.label} register</h2><p className="mt-1 text-xs text-slate-500">{visible.length} visible record{visible.length === 1 ? '' : 's'} · Relationships are linked by name and area.</p></div><div className="flex items-center gap-2"><SlidersHorizontal size={15} className="text-slate-400" /><select value={filter} onChange={(event) => setFilter(event.target.value)} className="h-9 rounded-lg border border-slate-200 bg-white px-3 text-xs" aria-label="Filter by status"><option>All</option>{statuses.map((status) => <option key={status}>{status}</option>)}</select></div></div>{visible.length ? <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{config.columns.map((column) => <th key={column} className="px-5 py-3 font-semibold">{column}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{visible.map((record) => <tr key={record.id} className="hover:bg-slate-50">{config.columns.map((column) => <td key={column} className="px-5 py-4 text-slate-700">{String(record[column] ?? '—')}</td>)}</tr>)}</tbody></table></div> : <div className="flex min-h-[300px] flex-col items-center justify-center px-6 text-center"><ClipboardList size={26} className="mb-4 text-teal-600" /><h2 className="text-lg font-semibold">No matching {config.label.toLowerCase()}</h2><p className="mt-2 max-w-md text-sm text-slate-500">Use Add {config.singular} to create a linked local record, or adjust your search and filter.</p></div>}</section> }

function Reports({ records, defaultType }: { records: Record<string, RecordItem[]>; defaultType: string }) { const [type, setType] = useState(defaultType); const [project, setProject] = useState('All projects'); const [area, setArea] = useState('All areas'); const [dateRange, setDateRange] = useState('All dates'); const [generated, setGenerated] = useState(false); const projects = records.Projects ?? []; const areas = records['Geographic Areas'] ?? []; const matchesScope = (item: RecordItem) => (project === 'All projects' || item.project === project || item.name === project) && (area === 'All areas' || item.area === area); const scoped = (name: string) => (records[name] ?? []).filter(matchesScope); const projectRows = projects.filter(matchesScope); const stakeholders = scoped('Stakeholders'); const engagements = scoped('Engagements'); const grievances = scoped('Grievances'); const indicators = scoped('Impact Monitoring'); const report = type === 'Project Summary' ? { heading: 'Project Summary Report', intro: 'Project details, geographic area, stage and current status.', rows: projectRows.map((item) => [['Project', item.name], ['Geographic area', item.area], ['Stage', item.stage], ['Status', item.status]]) } : type === 'Stakeholder Engagement' ? { heading: 'Stakeholder Engagement Report', intro: 'Stakeholder totals, engagements, participation and recent activities.', rows: [[['Stakeholder total', stakeholders.length], ['Engagements', engagements.length], ['Participation', engagements.some((item) => item.participants) ? engagements.map((item) => item.participants).join(', ') : 'Unavailable'], ['Recent activities', engagements.slice(-3).map((item) => item.title).join(', ') || 'Unavailable']] ] } : type === 'Grievance' ? { heading: 'Grievance Report', intro: 'Grievance totals, status, priority and resolution information.', rows: [[['Total grievances', grievances.length], ['Open', grievances.filter((item) => String(item.status).toLowerCase() === 'open').length], ['Closed', grievances.filter((item) => String(item.status).toLowerCase() === 'closed').length], ['Resolution details', grievances.some((item) => item.resolution) ? grievances.map((item) => item.resolution).join('; ') : 'Unavailable']] ] } : type === 'Impact Monitoring' ? { heading: 'Impact Monitoring Report', intro: 'Available indicators and recorded measurements from the prototype.', rows: indicators.map((item) => [['Indicator', item.name], ['Project', item.project], ['Measurement', `${item.value ?? 'Unavailable'}${item.unit ? ` ${item.unit}` : ''}`], ['Status', item.status], ['Measured on', item.measuredOn ?? 'Unavailable']]) } : { heading: 'Geographic Area Report', intro: 'Projects, stakeholders and activities summarised by geographic area.', rows: (area === 'All areas' ? areas : areas.filter((item) => item.name === area)).map((item) => { const name = String(item.name); return [['Area', name], ['Projects', projects.filter((row) => row.area === name).length], ['Stakeholders', (records.Stakeholders ?? []).filter((row) => row.area === name).length], ['Activities', (records.Engagements ?? []).filter((row) => row.area === name).length]] }) }; return <section className="flex flex-col gap-6"><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><h2 className="text-sm font-semibold">Report builder</h2><p className="mt-1 text-xs text-slate-500">Generate readable previews from current workspace records. No report figures are invented.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => window.print()} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-700">Print report</button><button disabled className="rounded-lg border border-dashed border-slate-300 px-3 py-2 text-xs font-semibold text-slate-400" title="Export is reserved for a future file export workflow">Export (limited)</button></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><label className="text-xs font-semibold text-slate-600">Report type<select value={type} onChange={(event) => { setType(event.target.value); setGenerated(false) }} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>Project Summary</option><option>Stakeholder Engagement</option><option>Grievance</option><option>Impact Monitoring</option><option>Geographic Area</option></select></label><label className="text-xs font-semibold text-slate-600">Project<select value={project} onChange={(event) => { setProject(event.target.value); setGenerated(false) }} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>All projects</option>{projects.map((item) => <option key={String(item.id)}>{String(item.name)}</option>)}</select></label><label className="text-xs font-semibold text-slate-600">Geographic area<select value={area} onChange={(event) => { setArea(event.target.value); setGenerated(false) }} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>All areas</option>{areas.map((item) => <option key={String(item.id)}>{String(item.name)}</option>)}</select></label><label className="text-xs font-semibold text-slate-600">Date range<select value={dateRange} onChange={(event) => { setDateRange(event.target.value); setGenerated(false) }} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>All dates</option><option>Last 30 days</option><option>Last 90 days</option></select></label></div><button onClick={() => setGenerated(true)} className="mt-5 inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-[#147d76] px-4 text-sm font-semibold text-white hover:bg-[#106b65]"><FileBarChart size={16} /> Generate report</button></div>{generated && <article className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm print:border-0 print:shadow-none"><div className="border-b border-slate-200 pb-5"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-teal-700">TrustLedger SRM · Generated preview</p><h2 className="mt-2 text-xl font-semibold text-slate-950">{report.heading}</h2><p className="mt-1 text-sm text-slate-500">{report.intro}</p><p className="mt-3 text-xs text-slate-400">Scope: {project} · {area} · {dateRange}</p></div>{report.rows.length ? <div className="mt-6 flex flex-col gap-4">{report.rows.map((row, index) => <div key={index} className="grid gap-3 rounded-lg border border-slate-100 bg-slate-50 p-4 sm:grid-cols-2 lg:grid-cols-4">{row.map(([label, value]) => <div key={label}><p className="text-[11px] font-semibold uppercase tracking-wide text-slate-400">{label}</p><p className="mt-1 text-sm font-medium text-slate-800">{String(value || 'Unavailable')}</p></div>)}</div>)}</div> : <div className="mt-6 rounded-lg border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500">No records match the selected filters. The report does not contain invented data.</div>}<p className="mt-6 text-xs text-slate-400">Generated from local prototype data. Export is currently limited and no production backend is connected.</p></article>}</section> }

function SettingsView({ preferences, setPreferences, records, setRecords }: { preferences: { compact: boolean; notifications: boolean; defaultReport: string }; setPreferences: (value: { compact: boolean; notifications: boolean; defaultReport: string }) => void; records: Record<string, RecordItem[]>; setRecords: (value: Record<string, RecordItem[]>) => void }) { return <section className="grid gap-6 lg:grid-cols-[1fr_340px]"><div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><div className="rounded-lg bg-teal-50 p-2.5 text-teal-700"><Settings size={19} /></div><div><h2 className="text-sm font-semibold">Workspace preferences</h2><p className="mt-1 text-xs text-slate-500">Preferences are saved locally in this browser.</p></div></div><div className="mt-7 flex flex-col gap-5"><label className="flex items-center justify-between gap-4 rounded-lg border border-slate-100 p-4"><span><span className="block text-sm font-medium">Compact density</span><span className="mt-1 block text-xs text-slate-500">Use smaller type for dense registers.</span></span><input type="checkbox" checked={preferences.compact} onChange={(event) => setPreferences({ ...preferences, compact: event.target.checked })} /></label><label className="flex items-center justify-between gap-4 rounded-lg border border-slate-100 p-4"><span><span className="block text-sm font-medium">Workspace notifications</span><span className="mt-1 block text-xs text-slate-500">Show the notification indicator in the header.</span></span><input type="checkbox" checked={preferences.notifications} onChange={(event) => setPreferences({ ...preferences, notifications: event.target.checked })} /></label><label className="flex flex-col gap-2 rounded-lg border border-slate-100 p-4 text-sm font-medium">Default report type<select value={preferences.defaultReport} onChange={(event) => setPreferences({ ...preferences, defaultReport: event.target.value })} className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>Project Summary</option><option>Stakeholder Engagement</option><option>Grievance</option><option>Impact Monitoring</option><option>Geographic Area</option></select></label></div></div><div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><Trash2 size={18} className="text-rose-600" /><h2 className="text-sm font-semibold">Data controls</h2></div><p className="mt-3 text-sm leading-6 text-slate-500">Clear all locally saved records and return to the original prototype dataset. This cannot be undone.</p><button onClick={() => { if (window.confirm('Clear all local records and restore sample data?')) setRecords(Object.fromEntries(configs.map((config) => [config.label, config.seed ?? []]))) }} className="mt-5 rounded-lg border border-rose-200 px-4 py-2 text-sm font-semibold text-rose-700 hover:bg-rose-50">Restore sample data</button><div className="mt-6 border-t border-slate-100 pt-5"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">Record inventory</p><p className="mt-2 text-2xl font-semibold">{Object.values(records).reduce((sum, list) => sum + list.length, 0)}</p><p className="mt-1 text-xs text-slate-500">records currently stored locally</p></div></div></section> }

export default TrustLedgerShell
