'use client'

import { useEffect, useMemo, useState } from 'react'
import { Activity, AlertCircle, Bell, ClipboardList, FileBarChart, FolderKanban, Gauge, Globe2, Handshake, LayoutDashboard, Menu, Network, Plus, Search, Settings, ShieldCheck, Users, X } from 'lucide-react'
import { RecordForm, type Field } from './record-form'

type RecordItem = { id: number; status?: string; [key: string]: string | number | undefined }
type ModuleConfig = { label: string; icon: typeof LayoutDashboard; singular: string; fields: Field[]; columns: string[]; seed?: RecordItem[] }

const configs: ModuleConfig[] = [
  { label: 'Projects', icon: FolderKanban, singular: 'project', fields: [{ key: 'name', label: 'Project name', placeholder: 'Bluewater Solar Corridor', required: true }, { key: 'area', label: 'Geographic area', placeholder: 'Mahlangu District', required: true }, { key: 'stage', label: 'Stage', placeholder: 'Planning', required: true }, { key: 'status', label: 'Status', placeholder: 'On track', required: true }], columns: ['name', 'area', 'stage', 'status'], seed: [{ id: 1, name: 'North Ridge Wind Farm', area: 'Mahlangu District', stage: 'Construction', status: 'On track' }] },
  { label: 'Geographic Areas', icon: Globe2, singular: 'area', fields: [{ key: 'name', label: 'Area name', placeholder: 'Kopano Municipality', required: true }, { key: 'region', label: 'Region', placeholder: 'Northern Corridor', required: true }, { key: 'notes', label: 'Notes', placeholder: 'Primary programme area' }], columns: ['name', 'region', 'notes'] },
  { label: 'Stakeholders', icon: Users, singular: 'stakeholder', fields: [{ key: 'name', label: 'Organisation name', placeholder: 'Riverbend Forum', required: true }, { key: 'type', label: 'Stakeholder type', placeholder: 'Community forum', required: true }, { key: 'area', label: 'Area', placeholder: 'Kopano Municipality', required: true }, { key: 'status', label: 'Relationship status', placeholder: 'Active', required: true }], columns: ['name', 'type', 'area', 'status'], seed: [{ id: 1, name: 'Riverbend Forum', type: 'Community forum', area: 'Kopano Municipality', status: 'Active' }] },
  { label: 'Governance Institutions', icon: Network, singular: 'institution', fields: [{ key: 'name', label: 'Institution name', placeholder: 'Kopano Municipal Council', required: true }, { key: 'level', label: 'Government level', placeholder: 'Municipal', required: true }, { key: 'contact', label: 'Primary contact role', placeholder: 'Planning office' }], columns: ['name', 'level', 'contact'] },
  { label: 'Engagements', icon: Handshake, singular: 'engagement', fields: [{ key: 'title', label: 'Engagement title', placeholder: 'Ward 4 construction briefing', required: true }, { key: 'project', label: 'Project', placeholder: 'North Ridge Wind Farm', required: true }, { key: 'date', label: 'Date', placeholder: '2026-09-24', type: 'date', required: true }, { key: 'status', label: 'Status', placeholder: 'Scheduled', required: true }], columns: ['title', 'project', 'date', 'status'] },
  { label: 'Grievances', icon: AlertCircle, singular: 'grievance', fields: [{ key: 'title', label: 'Issue summary', placeholder: 'Access road dust concerns', required: true }, { key: 'project', label: 'Project', placeholder: 'Cedar Valley Water Network', required: true }, { key: 'priority', label: 'Priority', placeholder: 'Medium', required: true }, { key: 'status', label: 'Status', placeholder: 'Open', required: true }], columns: ['title', 'project', 'priority', 'status'], seed: [{ id: 1, title: 'Access road dust concerns', project: 'Cedar Valley Water Network', priority: 'High', status: 'Open' }] },
  { label: 'Impact Monitoring', icon: Activity, singular: 'indicator', fields: [{ key: 'name', label: 'Indicator name', placeholder: 'Local employment', required: true }, { key: 'project', label: 'Project', placeholder: 'North Ridge Wind Farm', required: true }, { key: 'value', label: 'Current value', placeholder: '68%', required: true }, { key: 'status', label: 'Status', placeholder: 'On track', required: true }], columns: ['name', 'project', 'value', 'status'] },
]
const nav = [{ label: 'Dashboard', icon: LayoutDashboard }, ...configs.map(({ label, icon }) => ({ label, icon })), { label: 'Reports', icon: FileBarChart }, { label: 'Settings', icon: Settings }]

export function TrustLedgerShell() {
  const [active, setActive] = useState('Dashboard'); const [mobileOpen, setMobileOpen] = useState(false); const [query, setQuery] = useState(''); const [modal, setModal] = useState(false)
  const [records, setRecords] = useState<Record<string, RecordItem[]>>(() => Object.fromEntries(configs.map((config) => [config.label, config.seed ?? []])))
  const [hydrated, setHydrated] = useState(false); const [saveState, setSaveState] = useState<'idle' | 'saving' | 'saved' | 'error'>('idle')
  const config = configs.find((item) => item.label === active)
  useEffect(() => {
    try {
      const stored = window.localStorage.getItem('trustledger.records.v1')
      if (stored) setRecords(JSON.parse(stored))
    } catch { setSaveState('error') } finally { setHydrated(true) }
  }, [])
  useEffect(() => {
    if (!hydrated) return
    setSaveState('saving')
    try { window.localStorage.setItem('trustledger.records.v1', JSON.stringify(records)); setSaveState('saved') } catch { setSaveState('error') }
  }, [records, hydrated])
  const addRecord = (record: Record<string, string>) => { if (!config) return; setRecords((current) => ({ ...current, [config.label]: [...(current[config.label] ?? []), { ...record, id: Date.now() }] })); setModal(false) }
  const title = active === 'Dashboard' ? 'Good morning, Alex' : active; const description = active === 'Dashboard' ? 'Here’s your stakeholder relations overview for today.' : `Manage and monitor ${active.toLowerCase()} across your programmes.`
  return <div className="min-h-screen bg-[#f7f9fb] text-slate-900">
    <aside className={`fixed inset-y-0 left-0 z-30 flex w-64 flex-col border-r border-slate-200 bg-[#102a43] text-slate-200 transition-transform lg:translate-x-0 ${mobileOpen ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex h-20 items-center justify-between border-b border-white/10 px-6"><div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-lg bg-teal-400 text-[#102a43]"><Gauge size={21} /></div><div><p className="text-[17px] font-semibold text-white">TrustLedger</p><p className="text-[10px] font-medium uppercase tracking-[0.18em] text-teal-300">SRM · V2 Experimental</p></div></div><button onClick={() => setMobileOpen(false)} className="lg:hidden" aria-label="Close navigation"><X size={20} /></button></div><nav className="flex-1 px-3 py-6" aria-label="Main navigation"><p className="px-3 pb-3 text-[10px] font-semibold uppercase tracking-[0.16em] text-slate-500">Workspace</p><div className="flex flex-col gap-1">{nav.map((item) => { const Icon = item.icon; return <button key={item.label} onClick={() => { setActive(item.label); setMobileOpen(false) }} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm ${active === item.label ? 'bg-teal-400 font-semibold text-[#102a43]' : 'font-medium text-slate-300 hover:bg-white/10 hover:text-white'}`}><Icon size={17} /><span>{item.label}</span>{item.label !== 'Dashboard' && records[item.label] && <span className="ml-auto text-[10px] opacity-60">{records[item.label].length}</span>}</button> })}</div></nav><div className="border-t border-white/10 p-4"><div className="rounded-lg bg-white/5 p-3"><div className="flex items-center gap-2 text-xs font-medium text-teal-300"><ShieldCheck size={14} /> Prototype environment</div><p className="mt-1.5 text-[11px] leading-4 text-slate-400">Fictional sample data only. No live connections.</p></div></div></aside>
    <div className="lg:pl-64"><header className="sticky top-0 z-20 flex h-20 items-center justify-between border-b border-slate-200 bg-white/95 px-5 backdrop-blur sm:px-8"><div className="flex items-center gap-3"><button onClick={() => setMobileOpen(true)} className="text-slate-500 lg:hidden" aria-label="Open navigation"><Menu size={22} /></button><div><p className="text-xs font-medium text-slate-400">TrustLedger SRM <span className="mx-1">/</span> {active}</p><p className="mt-1 text-sm font-semibold text-slate-800">V2 Experimental Build</p></div></div><div className="flex items-center gap-4"><div className="relative hidden md:block"><Search className="absolute left-3 top-2.5 text-slate-400" size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search workspace" className="h-9 w-56 rounded-lg border border-slate-200 bg-slate-50 pl-9 pr-3 text-sm outline-none focus:border-teal-400" aria-label="Search workspace" /></div><Bell size={19} className="text-slate-500" /><span className={`hidden text-[11px] font-medium sm:inline ${saveState === 'error' ? 'text-rose-600' : saveState === 'saving' ? 'text-amber-600' : 'text-slate-400'}`} role="status">{saveState === 'error' ? 'Save error' : saveState === 'saving' ? 'Saving…' : hydrated ? 'Saved locally' : 'Loading records…'}</span><div className="flex size-8 items-center justify-center rounded-full bg-[#d9f0ed] text-xs font-bold text-[#147d76]">AM</div></div></header><main className="mx-auto max-w-[1440px] px-5 py-8 sm:px-8"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-sm font-medium text-teal-700">Tuesday, 17 September 2026</p><h1 className="text-2xl font-semibold tracking-tight text-slate-950">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p></div>{config && <button onClick={() => setModal(true)} className="inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-[#147d76] px-4 text-sm font-semibold text-white hover:bg-[#106b65]"><Plus size={16} /> Add {config.singular}</button>}</div>{active === 'Dashboard' ? <Dashboard records={records} onNavigate={setActive} /> : active === 'Reports' ? <Reports records={records} /> : config ? <ModuleView config={config} records={records[active] ?? []} query={query} onAdd={() => setModal(true)} /> : <Empty module={active} />}</main></div>{modal && config && <RecordForm title={`Add ${config.singular}`} fields={config.fields} onCancel={() => setModal(false)} onSave={addRecord} />}</div>
}

function Dashboard({ records, onNavigate }: { records: Record<string, RecordItem[]>; onNavigate: (value: string) => void }) { const activeProjects = records.Projects.length || 12; const stakeholders = records.Stakeholders.length || 1284; const grievances = records.Grievances.length || 38; const engagements = records.Engagements.length || 76; const cards = [{ label: 'Active Projects', value: activeProjects, Icon: FolderKanban, target: 'Projects' }, { label: 'Registered Stakeholders', value: stakeholders, Icon: Users, target: 'Stakeholders' }, { label: 'Open Grievances', value: grievances, Icon: AlertCircle, target: 'Grievances' }, { label: 'Engagements This Month', value: engagements, Icon: Handshake, target: 'Engagements' }]; return <div className="flex flex-col gap-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({ label, value, Icon, target }) => <button key={label} onClick={() => onNavigate(target)} className="rounded-xl border border-slate-200 bg-white p-5 text-left shadow-sm hover:border-teal-300"><div className="flex items-start justify-between"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-3 text-3xl font-semibold tracking-tight text-slate-950">{value}</p></div><div className="rounded-lg bg-teal-50 p-2.5 text-teal-700"><Icon size={19} /></div></div><p className="mt-3 text-xs font-medium text-slate-500">Select to manage records</p></button>)}</div><div className="grid gap-6 xl:grid-cols-2"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="text-sm font-semibold">Workspace activity</h2><p className="mt-1 text-xs text-slate-500">Recent locally logged records</p></div><ClipboardList size={18} className="text-teal-600" /></div><div className="mt-5 flex flex-col gap-3">{Object.entries(records).filter(([, items]) => items.length).slice(0, 5).map(([name, items]) => <div key={name} className="flex items-center justify-between rounded-lg bg-slate-50 px-3 py-3 text-sm"><span className="font-medium text-slate-700">{name}</span><span className="text-xs text-slate-500">{items.length} record{items.length === 1 ? '' : 's'}</span></div>)}</div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-semibold">Prototype guidance</h2><p className="mt-2 text-sm leading-6 text-slate-500">Use the module navigation to add fictional records. Changes are held in memory for this session and are not connected to a database.</p><div className="mt-5 flex items-center gap-2 text-xs font-semibold text-teal-700"><ShieldCheck size={15} /> Local-only data mode enabled</div></section></div></div> }

function Reports({ records }: { records: Record<string, RecordItem[]> }) {
  const [type, setType] = useState('Project Summary');
  const [project, setProject] = useState('All projects');
  const [area, setArea] = useState('All areas');
  const [dateRange, setDateRange] = useState('All dates');
  const [generated, setGenerated] = useState(false);
  const projects = records.Projects ?? [];
  const areas = records['Geographic Areas'] ?? [];
  const selectedProject = project === 'All projects' ? null : projects.find((item) => item.name === project);
  const projectRecords = selectedProject ? projects.filter((item) => item.name === selectedProject.name) : projects;
  const inScope = (item: RecordItem) => !selectedProject || item.project === selectedProject.name;
  const scopedStakeholders = (records.Stakeholders ?? []).filter((item) => !area || area === 'All areas' || item.area === area).filter(inScope);
  const scopedEngagements = (records.Engagements ?? []).filter(inScope);
  const scopedGrievances = (records.Grievances ?? []).filter(inScope);
  const scopedIndicators = (records['Impact Monitoring'] ?? []).filter(inScope);
  const report = type === 'Project Summary' ? { heading: 'Project Summary Report', intro: 'Project details, geographic area, stage and current status.', rows: projectRecords.map((item) => ['Project', item.name, 'Area', item.area, 'Stage', item.stage, 'Status', item.status]) } : type === 'Stakeholder Engagement' ? { heading: 'Stakeholder Engagement Report', intro: 'Stakeholder totals, engagements, participation and recent activities.', rows: [['Stakeholders', String(scopedStakeholders.length), 'Engagements', String(scopedEngagements.length), 'Participation', 'Not recorded', 'Recent activities', scopedEngagements.slice(-3).map((item) => item.title).join(', ') || 'Unavailable']] } : type === 'Grievance' ? { heading: 'Grievance Report', intro: 'Grievance totals, status, priority and resolution information.', rows: [['Total grievances', String(scopedGrievances.length), 'Open', String(scopedGrievances.filter((item) => item.status?.toLowerCase() === 'open').length), 'Closed', String(scopedGrievances.filter((item) => item.status?.toLowerCase() === 'closed').length), 'Resolution', 'Resolution details unavailable']] } : type === 'Impact Monitoring' ? { heading: 'Impact Monitoring Report', intro: 'Available indicators and recorded measurements from the prototype.', rows: scopedIndicators.map((item) => ['Indicator', item.name, 'Project', item.project, 'Measurement', item.value, 'Status', item.status]) } : { heading: 'Geographic Area Report', intro: 'Projects, stakeholders and activities summarised by geographic area.', rows: (area === 'All areas' ? areas : areas.filter((item) => item.name === area)).map((item) => { const name = String(item.name); return ['Area', name, 'Projects', String(projects.filter((row) => row.area === name).length), 'Stakeholders', String((records.Stakeholders ?? []).filter((row) => row.area === name).length), 'Activities', String((records.Engagements ?? []).filter((row) => row.area === name).length)] }) };
  return <section className="flex flex-col gap-6"><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><h2 className="text-sm font-semibold">Report builder</h2><p className="mt-1 text-xs text-slate-500">Generate a readable report preview from current workspace records. Figures update as records are added.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => window.print()} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-700">Print report</button><button disabled className="rounded-lg border border-dashed border-slate-300 px-3 py-2 text-xs font-semibold text-slate-400" title="Export is reserved for a future file export workflow">Export (limited)</button></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><label className="text-xs font-semibold text-slate-600">Report type<select value={type} onChange={(event) => setType(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>Project Summary</option><option>Stakeholder Engagement</option><option>Grievance</option><option>Impact Monitoring</option><option>Geographic Area</option></select></label><label className="text-xs font-semibold text-slate-600">Project<select value={project} onChange={(event) => setProject(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>All projects</option>{projects.map((item) => <option key={String(item.id)}>{String(item.name)}</option>)}</select></label><label className="text-xs font-semibold text-slate-600">Geographic area<select value={area} onChange={(event) => setArea(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>All areas</option>{areas.map((item) => <option key={String(item.id)}>{String(item.name)}</option>)}</select></label><label className="text-xs font-semibold text-slate-600">Date range<select value={dateRange} onChange={(event) => setDateRange(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal"><option>All dates</option><option>Last 30 days</option><option>Last 90 days</option><option>Year to date</option></select></label></div><button onClick={() => setGenerated(true)} className="mt-5 inline-flex h-10 items-center justify-center rounded-lg bg-[#147d76] px-4 text-sm font-semibold text-white hover:bg-[#106b65]">Generate report</button></div>{generated && <article className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm print:border-0 print:shadow-none"><div className="border-b border-slate-200 pb-4"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-teal-700">TrustLedger SRM · Generated preview</p><h2 className="mt-2 text-xl font-semibold text-slate-950">{report.heading}</h2><p className="mt-1 text-sm text-slate-500">{report.intro}</p><p className="mt-3 text-xs text-slate-400">Filters: {project} · {area} · {dateRange}</p></div><div className="mt-5 flex flex-col gap-3">{report.rows.length ? report.rows.map((row, index) => <div key={index} className="grid gap-2 rounded-lg bg-slate-50 p-4 sm:grid-cols-4">{row.map((cell, cellIndex) => <div key={cellIndex}><p className="text-[10px] font-semibold uppercase tracking-wide text-slate-400">{cellIndex % 2 === 0 ? cell : 'Value'}</p>{cellIndex % 2 === 1 && <p className="mt-1 text-sm font-medium text-slate-800">{cell || 'Unavailable'}</p>}</div>)}</div>) : <p className="rounded-lg bg-slate-50 p-5 text-sm text-slate-500">No records match the selected filters. The report does not invent data.</p>}</div></article>}</section>
}

function ModuleView({ config, records, query, onAdd }: { config: ModuleConfig; records: RecordItem[]; query: string; onAdd: () => void }) { const visible = useMemo(() => records.filter((record) => Object.values(record).some((value) => String(value ?? '').toLowerCase().includes(query.toLowerCase()))), [records, query]); return <section className="rounded-xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-4"><div><h2 className="text-sm font-semibold">{config.label} register</h2><p className="mt-1 text-xs text-slate-500">{visible.length} visible record{visible.length === 1 ? '' : 's'} · Search from the top bar to filter</p></div></div>{visible.length ? <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{config.columns.map((column) => <th key={column} className="px-5 py-3 font-semibold">{column}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{visible.map((record) => <tr key={record.id} className="hover:bg-slate-50">{config.columns.map((column) => <td key={column} className="px-5 py-4 text-slate-700">{record[column] || '—'}</td>)}</tr>)}</tbody></table></div> : <Empty module={config.label} onAdd={onAdd} />}</section> }
function Empty({ module, onAdd }: { module: string; onAdd?: () => void }) { return <div className="flex min-h-[360px] flex-col items-center justify-center px-6 text-center"><div className="mb-5 flex size-14 items-center justify-center rounded-2xl bg-teal-50 text-teal-700"><ClipboardList size={26} /></div><h2 className="text-lg font-semibold text-slate-900">No {module.toLowerCase()} records yet</h2><p className="mt-2 max-w-md text-sm leading-6 text-slate-500">Add a fictional record to start building this TrustLedger register.</p>{onAdd && <button onClick={onAdd} className="mt-6 inline-flex h-10 items-center gap-2 rounded-lg bg-[#147d76] px-4 text-sm font-semibold text-white"><Plus size={16} /> Add record</button>}</div> }
export default TrustLedgerShell
